import { PipeDegreePipe } from './pipe-degree.pipe';

describe('PipeDegreePipe', () => {
  it('create an instance', () => {
    const pipe = new PipeDegreePipe();
    expect(pipe).toBeTruthy();
  });
});
